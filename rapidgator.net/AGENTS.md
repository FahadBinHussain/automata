# rapidgator.net - no free-speed bypass (STATUS: do not use except tiny files)

> **2026-09-09 verdict:** we currently have NO bypass for rapidgator's slow
> free-download speed. rule: **do not use rapidgator for whatever task the
> agent is doing, unless the download size is tiny.**

## verified 2026-09-09 (RLSBB hop for LIMSCAPE.THE.LIMINAL.SPACE.EXPLORER-TENOKE)

- file alive: `L.T.L.S.E-TENOKE.rar`, 1.41 GB, page 200 with "Download file" +
  countdown "download will start in ~113 seconds", no captcha at gate.
- free path = ~2 min wait, then speed-capped free transfer (no premium).
  nitroflare + fikper + usersdrive + dailyuploads mirrors of the same release
  were all 404/blocked — rapidgator was the last alive hop and still not worth it.
- chain note: the RLSBB post (`post.rlsbb.ru/limscape-...-tenoke`) is the
  in-chain source (FMHY /gaming -> RLSBB); rapidgator is only its hoster.

## what this means for future tasks

- if a release is ONLY on rapidgator (free), prefer another source:
  another hoster mirror, a repack (fitgirl/dodi), 1337x trusted-uploader
  torrent, or ask the user before burning time on the countdown + trickle.
- tiny files (a few MB: cracks, fixes, nfos) are fine — the wait dominates,
  not the transfer.
- never pay for premium / enter accounts without the user explicitly saying so
  (spending rule: paid options get flagged loudly first).
