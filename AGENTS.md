# automata folder conventions

this directory holds reusable local automation scripts, keyed by the service/site they automate.

## folder naming rule

every subfolder name MUST be a URL-style hostname (domain) of the service it targets, for example:

- `youtube.com`
- `whatsapp.com`
- `google.com`
- `marketplace.visualstudio.com`
- `wall.alphacoders.com`
- `files.vc`

rules:
- use the bare domain/hostname only, lowercase, no scheme, no path, no trailing slash
- multi-label hosts keep their subdomains (e.g. `marketplace.visualstudio.com`, not `visualstudio.com`)
- if a service has no clear web hostname, use its canonical public domain when one exists (e.g. `bitwarden.com`), otherwise ask before creating a non-URL folder name
- generic/non-host folders such as `tools` are allowed only for cross-service shared helpers, and must be clearly named as such; do not add site-specific automation under `tools`

## what goes in here
- one-off and reusable browser/admin/API automation scripts
- each script should have a short header comment or companion README with purpose, inputs, and run command
- never commit secrets, cookies, tokens, browser profiles, private screenshots, or downloaded chat media
- personal values (emails, jids, ips, macs, private urls, ids) go in `<folder>\.env.local` (gitignored via `**/.env.local`) and are read by the script's env loader; scripts should fall back to `<placeholder>` defaults, never hardcode personal data. docs use `C:\Users\<user>` / `$env:USERPROFILE`, not a real username.
- put throwaway scratch files in `C:\tmp`, not here

## current non-conforming folders (as of 2026-07-16)
- `bitwarden` -> should be `bitwarden.com`
- `murmur` -> no public hostname (private app); keep as-is until a canonical domain is decided
- `theoldllm` -> should be the service's domain once confirmed
- `tools` -> allowed as cross-service shared helpers (see rule above)

When the project is a single-file userscript, always copy the complete userscript to the clipboard after every update.

## bitwarden.com unlock gotchas (bitwarden.com/unlock-inner.ps1)

- **stale-key unlock failure** (2026-08-13): if `bw unlock --passwordenv BW_PASSWORD --raw` exits 1 with `bitwarden_crypto::keys::master_key: error=The decryption operation failed ... The provided key is not the expected type`, the stored encrypted keys in data.json don't match the typed master password - the master password changed on the server (the vaultwarden instance for this profile — URL kept out of the repo) after the local state was saved. `bw status` still reports `locked`, so the old flow silently retried 3x and looked stuck. unlock-inner now detects the error text, prints it, and runs a one-time `bw logout` + `bw login <email> --passwordenv` heal to rebuild local keys from the server (vault is server-side, nothing lost; 2FA prompt appears in the unlock window if enabled). first attempt after a server-side password change: expect the heal message, NOT a session.
- unlock is the master-password entrypoint and the flow is fully self-sufficient: unlock-inner handles `bw login` (unauthenticated state), the stale-key heal (logout + re-login), and session persistence by itself - no mainframe bitwarden helper exists anymore (retired 2026-08-17, single-account setup; multi-account data.json swapping was dead weight). session/profile store: `%APPDATA%\mainframe\accounts\bitwarden\` (session.key + `<email>\profile.json` for the login email). the scoop-persisted `bw-data\data.json` is the only local CLI state and is disposable - the vault lives server-side, re-login rebuilds it.
- **stale-key heal side effect (2026-08-13)**: the heal's `bw logout` can shrink the live `data.json` to ~13KB (global_clearEvent_logout) until the re-login rebuilds it - that is expected, do not "restore" anything; never run `bw sync` inside the unlock window.
- **agents must pass the session explicitly** - this CLI build (2026.5.0 SDK) does NOT persist the unlock across processes: bare `bw status` (no env) reports `locked` even right after a successful unlock. the standard agent flow is: `unlock.ps1` (interactive window) -> `get-session.ps1` (validates + prints the key) -> `$env:BW_SESSION = <key>` -> `bw ...` (or use `--session`). find-note.ps1 follows this; never assume a plain `bw` call is unlocked.
- **bw output encoding trap (2026-08-13)**: `bw` prints UTF-8, but agent shells on this machine can have `[Console]::OutputEncoding = ibm437`, so non-ASCII (Bengali notes, emoji, curly apostrophes) reads back as mojibake (`ΓÇÖ`, `αªª`, `≡ƒÿ▒`). this looks like corrupt vault data but is a READ-path artifact; the reverse is `[Text.Encoding]::UTF8.GetString([Text.Encoding]::GetEncoding(437).GetBytes($bad))` (repeat until no CP437 glyphs remain). NEVER write a mis-read string back without re-encoding first - a JSON round-trip through a mojibake'd read can corrupt real data. always start scripts with `[Console]::OutputEncoding = [System.Text.Encoding]::UTF8` (find-note.ps1 / get-session.ps1 already do).
- **mojibake hunt = byte-level, never console-read** (2026-08-13): console `bw list/get` scans can be BLIND to real stored mojibake - a CP437 console decodes the stored `ΓÇÖ` bytes (CE 93 C3 87 C3 96) into different glyphs, so string-matching for `ΓÇ`/`ÇÖ` finds nothing and the vault looks clean while the extension still shows garbage. the correct audit is: `bw export --format json --output <file>` (bw writes the file directly, no console in between) then scan the RAW BYTES for the mojibake sequences `CE 93` (Γ), `C3 87` (Ç), `C3 96` (Ö), `C3 B6`, `C2 AA`, `CE B1`, `E2 89 A1`, `E2 96 91/92`, `E2 94 AC`, `EF BF BD` (U+FFFD), `E2 80 AA/AB/AC/A2` (bidi/bullets); parse the export with `Get-Content -Raw -Encoding utf8` (note: root object has `.items`, not a bare array) and fix each hit with the CP437-reverse + `bw edit item <id> <base64-json>`. done 2026-08-13: 4 items (multcloud.com, paypal.com, qb.eshiksabd.com, www.apple.com) had real `ΓÇÖ`-type corruption, fixed 1 pass each, re-export verified 0 hits across all 13 patterns.
- `bw edit item <id> <base64-json>` is the CURRENT syntax (2026.5.0 SDK CLI) - the old `bw item edit <id> --notes ...` subcommand is GONE and multi-line args break anyway; build JSON with ConvertTo-Json, base64 it, pass as one arg.

## youtube neon sync userscript gotchas (youtube.com/youtube.com-watch-progress-tracker-with-neon-cloud-sync)

- flush() must never abort the loop on the first failing row - one persistently failing row (rate limit, bad content) starved every row behind it forever. each row gets its own attempt per pass; only failing rows stay in the dirty queue.
- never sweep the whole cache into the dirty set at sync time - a 200-row re-upload every 5s of watching trips neon's rate limit and causes the failures above. only actual dirty rows get pushed.
- pagehide/beforeunload must record(true) AND flush() - the debounced push never fires when the tab dies, so the final position is lost until the next visit.
- finished semantics are strict and last-write-wins everywhere: done only comes from the player's own signals (`ended` / ended-mode / clock at duration / seeked landing within 0.5s of the seekable end). even 1s left = green. a rewind recorded after a finish CLEARS the done mark - the SQL upsert uses `finished = excluded.finished` (not OR-sticky), and the pull merge trusts only the NEWER row's flag near the end, not `(remote || local)`. the ONLY protection is client-side in record(): `unload`/`pagehide` writes near the end keep done=true so teardown noise cannot revert a watched video. legacy rows flagged finished at 90% (old builds) get cleaned by the same near-end rule and re-pushed to converge the DB.
- sync status lives in the floating panel + console (autoSync logs reason); connection string is a Neon role scoped to `watch_progress` only, never the owner role.
- the panel has a persistent event Log tab (GM storage `progressLog`, 500 entries): every record (with source: tick/pause/ended/seeked/poller/autosync/unload/pagehide), flush fail, merge change, loadfix clear, and seek is logged - use it to trace progress reverts instead of guessing.
- ~~merged music tracker~~ (0.6.0+): the spotify/soundcloud visited-track userscript (old `spotify.com/spotify-track-history-tracker-and-smart-youtube-search-integration`) is now the same file, gated by `IS_YT` / `IS_MUSIC`; both sides share one neon conn string (settings panel is on the youtube side, `connStr()` is shared) and one `neonQuery`. the old google apps script backend is DEAD (`script.googleusercontent.com` echo returns 404, cold start ~43s > 15s timeout) - visited history now lives in neon `visited_tracks (id text pk, name, source check('spotify','soundcloud','ytmusic'), updated_at)`. backfilled 1198 spotify + 4 soundcloud rows from the google sheet "Spotify History" (`1xWa2uDiUAMwQLTtOXAEb531rpmCOPyhTOSKJnTsdZOE`, tabs Spotify/SoundCloud, cols A=id B="artist - song" C=US-locale timestamp) using `on conflict (id) do nothing` + separate UPDATE pass; sheet kept untouched, nothing deleted.
- music module details: `vtrackId` normalizes track ids (leading slashes stripped both on save and fetch); `mFetchHistory()` replaces the local set from the cloud per source; `mSave()` inserts with `on conflict (id) do update set name = excluded.name, updated_at = now()` (upsert) or deletes; offline banner + retry with exponential backoff when conn string missing/fetch fails. GM storage keys for the music side are `mVisitedSet`, `mVisitedCache`, `mVisitedLog` (must never collide with youtube-side keys). 0.7.0 added music.youtube.com: same YT search buttons mount on track rows (`ytmusic-responsive-list-item-renderer` / `ytmusic-queue-item-renderer`) and the now-playing bar, track id = the video id (`watch?v=` param or `video-id` attr), source `ytmusic`. existing DBs auto-migrate once via `mEnsureYtMusicSource()` (recreates the `visited_tracks_source_check` constraint to allow 'ytmusic', idempotent, keyed by GM `mSourceMigrated`) - Create-table button + `schema.sql` already ship the new constraint for fresh installs. **trusted types gotcha (0.7.1)**: music.youtube.com enforces TrustedHTML (like www.youtube.com) - the old `btn.innerHTML = <svg>` in `mUpdateIcon` threw "Failed to set the 'innerHTML' property", and because the throw happened inside `mFetchHistory()`'s `.then` (which calls `mAddAllButtons()`), it was caught by that promise's `.catch` and flipped the whole music tracker to the offline banner. icons are now injected via `mSvgEl()`. **trusted types gotcha (0.7.3)**: music.youtube.com blocks DOMParser.parseFromString TOO - even `image/svg+xml` throws "Failed to execute 'parseFromString' on 'DOMParser': This document requires 'TrustedHTML' assignment" (that killed every icon with no banner because the per-row try/catch swallowed it; caught only via the `ytmusic row mount failed:` mlog lines). `mSvgEl()` now builds the svg node by hand with `document.createElementNS` + a regex over the svg open tag and the path `d` attr (createElementNS is not a Trusted Types sink; works on all sites). never reintroduce innerHTML or DOMParser in the music side. **ytmusic row mount gotcha (0.7.2)**: on the new flex-columns playlist/search UI (`ytmusic-responsive-list-item-renderer` with `secondary-flex-columns` JSON attr, `ellipsis-truncate` titles) inserting the button beforebegin of the title `<a>` put it INSIDE the truncated `yt-formatted-string.title`, which clips/re-renders and hid it; mount = append into `.title-column` (or `.track-title`'s parent / title's parent) with flex display. artist for that UI comes from the first `.secondary-flex-columns .flex-column yt-formatted-string` (the byline classes are gone) - **CORRECTED 0.7.4**: the artist actually lives inside the `secondary-flex-columns` JSON *attribute* on the renderer (`[{text:{runs:[...]}}]`), not in DOM classes; `mYTMArtistFromFlex()` parses that attribute and picks runs whose `navigationEndpoint` has `browseEndpointContextMusicConfig.pageType == "MUSIC_PAGE_TYPE_ARTIST"` (fallback: browseId starts with `UC`). without it the YT search button URL fell back to song-only (author missing from the youtube search). every row mount is try/catch'd so one bad row can't abort the pass and re-trigger the offline banner via mFetchHistory's .catch.
- the settings "Create table" button creates `watch_progress` + `ui_prefs` + `visited_tracks` - keep all three in sync with `schema.sql`.
- resume override is ours ONLY when our position >= 1s: retime() stamps t= + ytpT on the thumbnail, the click stashes pendingSeek, applySeek seeks. when our position is 0, we must defer to youtube's own resume - retime() strips any stamp we added earlier (delete t= + ytpT) and applySeek() drops a stale pendingSeek when cache position is 0, so a rewind-to-0 or never-watched video can't get a stale injected timestamp. never "fix" this by stamping t=0.

## steam shortcuts vdf gotchas (steampowered.com/steam-shortcuts.ps1)

- the whole `shortcuts.vdf` file IS a single set named `shortcuts` (`0x00 shortcuts 0x00 <entries> 0x08`) - there is NO outer wrapper set, so a reader must consume the root header directly; parsing it as a nested set makes the code expect a phantom 4th `0x08` and read past EOF (`$bytes[i]` returns null past the end, then `.ToString()` throws "cannot call a method on a null-valued expression").
- entry keys are `0x00 <index> 0x00` sets; fields are `0x01` (string: key, nul, value, nul) and `0x02` (int: key, nul, 4 bytes); `0x08` closes a set.
- some third-party writers emit a stray trailing `0x08`; tolerate EOF in the set loop (break when `Pos -ge Length`) so both formats parse.
- PowerShell `[ordered]@{}` (OrderedDictionary) has `.Contains()`, NOT `.ContainsKey()` - any `.ContainsKey()` call on parsed entries throws "method invocation failed".
- round-trip invariant: parse -> write with unchanged entries must be byte-identical; verify by comparing bytes before/after an add-then-remove on a temp copy.
- steam must be closed while writing, or it overwrites the file on exit; the script backs up to `shortcuts.vdf.bak-<timestamp>` before every write.
- WRITER MUST MATCH STEAM'S BYTES EXACTLY (nightly client 20260706+): a structurally-off file makes `LoadShortcuts` fail at every boot (`usershortcuts.cpp (131) : CSteamDoc::LoadShortcuts: failed to load shortcut file` in `logs\console_log.txt`) and Steam then renames `shortcuts.vdf` to `logs\shortcuts.previous.txt` (quarantine, rename preserves LastWriteTime). symptoms looked like an "external eater" - it's just Steam quarantining on parse failure. exact rules learned from diffing against a Steam-UI-written file: (1) the file must end with FOUR `0x08` closers after `tags 00` (`tags 00 08 08 08 08`) - three is rejected; (2) `StartDir` is written UNQUOTED and WITH trailing backslash (`C:\...\Win64\`), while `Exe` IS quoted; (3) appid = uint32 with high bit set (`0x80000000 | random31`) - small/negative ids get "Unknown GameID type" on rungameid URLs. verify any writer change with the round-trip test above against a UI-written file. note: `steam://rungameid/<id>` URLs don't work for shortcuts in this client ("Unknown GameID type" from steamid.cpp even with the correct id) - launch from the library UI instead; UI adds get appid 0 and Steam sanitizes (`sanitize shortcut app id ... replacing 0 with <id>`) which works fine.

## alist + terabox + rclone crypt bridge (tools\alist-terabox\)
- **setup date**: 2026-08-24. **AList v3.63.0** (scoop, main bucket) patched with terabox driver fix (`getFiles` normalizes empty dir to `/` in `drivers/terabox/util.go`). needed because TeraBox migrated its API from `www.terabox.com` to `dm.terabox.com` (the driver has a redirect handler that reads `Url-Domain-Prefix: dm` header on errno -6, but the dm API rejects empty `dir` param with `errno: 2`). without the patch, listing returns 0 files -> WebDAV PUT fails.
- **patch**: `tools\alist-terabox\terabox-empty-dir.patch` (git format-patch). PR upstream: https://github.com/AlistGo/alist/pull/9630
- **patcher script**: `tools\alist-terabox\alist-terabox-patcher.ps1` — run after every `scoop update alist` to rebuild the official binary with the patch applied. Run unattended: `powershell ...\alist-terabox-patcher.ps1`.
- **build deps**: Go, git, scoop. The script clones the pinned version source, applies the patch, fetches frontend dist, builds, backs up the old binary, installs, restarts.
- **stack**: AList WebDAV (`http://localhost:5244/dav/`) <- rclone WebDAV remote `AListWebDAV` <- rclone crypt remote `TeraboxCrypt` (encrypts names + content). config: `$env:USERPROFILE\scoop\apps\rclone\current\rclone.conf`.
- **AList creds**: `admin` / stored in vault item `alist-local-admin` (password `MoUsdiOaSHuPI98Fxz6D`). TeraBox cookie: `ndus` + `browserid` + `lang` from logged-in session (captured via agent-browser). Use `download_api=official` (crack mode returns empty dlink on dm).
- **known issues**: (1) AList Terabox driver has open bugs: #7417 (WebDAV upload fails for large files), #8429 (files can't be deleted/renamed), #7301 (upload/delete broken). The patch fixes the listing issue (empty dir -> /) so WebDAV MKCOL+PUT works. (2) rclone `lsl`/`copy` may hang on large dirs due to the driver's download link generation; `rclone cat` works for single files. (3) the `_20260824_...` suffix dirs are garbage from failed MKCOL retries (TeraBox dedupe renames) — delete them.
- **direct API cleaner**: `tools\alist-terabox\terabox-clean.ps1` — bypasses AList's broken delete (bug #8429) by calling TeraBox's `/api/filemanager?opera=delete` directly with the session cookie, following the `dm` domain redirect. Supports `-All`, `-Names @(...)`, `-Prefixes @(...)`, `-KeepFiles`, `-DryRun`. Cookie lives at `$env:TEMP\terabox-cookie2.txt`.
- **root_folder_path gotcha (CRITICAL)**: the Terabox storage MUST have `root_folder_path="/"` (not empty). If it's empty, AList's path-propagation collapses — every subdir obj gets an empty GetPath(), so `getFiles()` is always called with `/` (root) and all WebDAV uploads FLATTEN to the root, plus subdir listings return root content. Symptom: rclone copy to nested paths lands everything at root, and `lsd` shows nested dirs (Avocado girl, PICS, FRVA, ...) at the top level. Fix via `POST /api/admin/storage/update` setting `root_folder_path: "/"`. Verified 2026-08-24 after first upload flattened all of "add to gg" (~1.5GB, wiped + re-uploaded cleanly).
- **rclone config**: AListWebDAV (webdav @ localhost:5244/dav/, admin/pass) + TeraboxCrypt (crypt wrapping AListWebDAV:TeraBox, keys in vault item `terabox.com[2]` notes).
- **AList restart**: `Stop-Process -Name alist; Start-Process pwsh -ArgumentList "-NoExit","-Command","& '$env:USERPROFILE\scoop\apps\alist\current\alist.exe' server --data '$env:USERPROFILE\scoop\persist\alist\data'"`
- **AList API token**: login at localhost:5244/api/auth/login with admin/pass, store in `$env:TEMP\alist-token.txt`.

## ditto fork build + patcher (github.com\sabrogden\Ditto\ditto-fork-build.ps1)
- **setup date**: 2026-08-24. **Ditto** (scoop, extras bucket, 3.25.113.0) patched with user's merged PRs. Ditto's latest release (3.25.113.0, 2025-09) predates the user's merged PRs: #1098 (starred clips filter) and #1107 (shortcut action for starred clips) - they're in upstream master but never released. The fork (FahadBinHussain/Ditto) is synced to upstream master and has the MSBuild workflow with a `workflow_dispatch` trigger (added manually) + Actions workflow permissions set to `write`.
- **build**: GitHub Actions on the fork builds master (`msbuild CP_Main_10.sln /p:Configuration=Release /p:Platform=x64`) and uploads an `unsigned-exe` artifact (Ditto.exe + ICU_Loader.dll + Addins\DittoUtil.dll). NOTE: the workflow's SignPath signing step fails on the fork (no `SIGNPATH_API_TOKEN` secret) - that's fine, the unsigned-exe artifact is uploaded BEFORE signing. The fork's build-tag-number step creates version tags like `build-number-136`.
- **patcher script**: `github.com\sabrogden\Ditto\ditto-fork-build.ps1` - triggers the fork build (or `-SkipBuild` to reuse `$env:TEMP\ditto-build`), downloads the unsigned-exe artifact, backs up the official binary (`Ditto.exe.bak-<timestamp>`), replaces it, restarts Ditto. Run after every `scoop update ditto`.
- **installed state**: `<user-home>\scoop\apps\ditto\current\Ditto.exe` = fork build 3.25.1.0 (patched 2026-08-24). Official backup: `Ditto.exe.bak-20260824-130423`.
- **data migration (2026-08-24)**: old clips DB + settings moved from the deleted portable build (`Downloads\softwares\DittoPortable_64bit_show_starred_clips`) into scoop persist. Old DB (2GB, was `Ditto_1.db`) is now `persist\ditto\Ditto.db`; `Ditto.Settings` carries past searches, window positions, fonts. `DBPath3` was rewritten to `Ditto.db` (scoop persist name). Pre-migration scoop DB+settings backed up to `persist\ditto\backup-scoop-db-20260824\`. Portable folder deleted 2026-08-24.
- **trigger the fork CI manually**: `gh workflow run build.yml --repo FahadBinHussain/Ditto --ref master`. The fork needs Actions workflow permissions = write (set via `gh api -X PUT repos/FahadBinHussain/Ditto/actions/permissions/workflow -F default_workflow_permissions=write`).
- **why a fork build, not a release**: upstream only cuts releases on its own schedule; the fork's Actions can't auto-publish a release (release step also needs secrets). So we build on the fork CI and patch the local scoop binary.

## steam shortcut LAUNCH route (steampowered.com/steam-launch.ps1)

- the library UI launches shortcuts (and real games) NOT via `steam://rungameid/` URLs but via a CEF bridge call in steamui JS: `SteamClient.Apps.RunGame(overview.gameid, "", -1, launchOptions)` - `gameid` is the string from the app overview (`window.appStore.GetAppOverviewByAppID(<decimal appid>).gameid`, e.g. PEAK 3861904653 -> gameid "16586754184938782720"), NOT the raw appid - raw appid or guessed type-prefixes are rejected/ignored.
- `steam://rungameid/<n>` and `steam://launch/<n>` and `-applaunch <n>` all die in `src\common\steamid.cpp (696) : Unknown GameID type` for shortcut appids in this client; `steam://rungameid/3861904653` gives "Game configuration unavailable" - the URL path treats the number as a GameID and rejects the shortcut encoding.
- external access to the bridge = CEF remote debugging: steam.exe MUST be started with `-cef-enable-debugging` (flag exists in the client); steamwebhelper then listens on `http://127.0.0.1:8080` (CDP). without the flag there is no port - if steam is already running without it, the bridge route is unavailable until restart. first boot after enabling can take minutes (htmlcache rebuild; "System startup time" showed ~580s once).
- CDP flow: `GET /json` -> find the target page titled `SharedJSContext` (the app's single-page context where window.SteamClient + window.appStore live; the supernav/menu pages are separate about:blank targets) -> `Runtime.evaluate` with `awaitPromise` on the RunGame expression. ready check: `window.appStore && window.SteamClient.Apps`.
- `steam-launch.ps1` wraps all of it: `.\steam-launch.ps1 -Game "<name>"` (auto-starts steam with the flag when it isn't running; `-Restart` auto-restarts a running flagless steam; `-List` prints names+appids). paired helper `steam-cdp-eval.mjs <wsUrl> <expression>` is a generic CDP evaluator.
- shortcuts whose exe no longer exists launch-fail with `LaunchApp failed with AppError_46` (log) - the bridge call itself succeeds (log shows `launched gameid=...`); verify exe paths with `Test-Path` before trusting a shortcut. e.g. gta-vc in the 5-game set is currently a dead entry (gta-vc.exe not on disk).
